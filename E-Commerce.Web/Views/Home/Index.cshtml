@{
    ViewBag.Title = "Premium Shopping Experience";
}

<style>
    :root {
        --primary-color: #1e40af;
        --primary-light: #3b82f6;
        --accent-color: #f59e0b;
        --text-dark: #1f2937;
        --text-light: #6b7280;
        --bg-light: #f9fafb;
        --border-color: #e5e7eb;
        --white: #ffffff;
        --shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
        --shadow-lg: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
    }

    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    body {
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
        line-height: 1.6;
        color: var(--text-dark);
    }

    .hero-section {
        position: relative;
        height: 70vh;
        background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-light) 100%);
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
        color: white;
        overflow: hidden;
    }

    .hero-content {
        z-index: 2;
        max-width: 600px;
        padding: 2rem;
    }

    .hero-title {
        font-size: 3.5rem;
        font-weight: 700;
        margin-bottom: 1rem;
        line-height: 1.2;
    }

    .hero-subtitle {
        font-size: 1.25rem;
        margin-bottom: 2rem;
        opacity: 0.9;
    }

    .cta-button {
        display: inline-block;
        padding: 1rem 2rem;
        background: var(--accent-color);
        color: white;
        text-decoration: none;
        border-radius: 8px;
        font-weight: 600;
        transition: all 0.3s ease;
        margin: 0 0.5rem;
    }

        .cta-button:hover {
            transform: translateY(-2px);
            box-shadow: var(--shadow-lg);
            color: white;
            text-decoration: none;
        }

        .cta-button.secondary {
            background: transparent;
            border: 2px solid white;
        }

    .section-padding {
        padding: 5rem 0;
    }

    .section-title {
        text-align: center;
        font-size: 2.5rem;
        font-weight: 700;
        margin-bottom: 3rem;
        color: var(--text-dark);
    }


    .countdown-timer {
        background: var(--accent-color);
        color: white;
        padding: 1rem 2rem;
        border-radius: 8px;
        text-align: center;
        margin-bottom: 2rem;
        display: inline-block;
    }

    .timer-text {
        font-size: 1.1rem;
        font-weight: 600;
    }

    .highlights-section {
        background: var(--primary-color);
        color: white;
    }

    .highlight-item {
        text-align: center;
        padding: 2rem 1rem;
    }

    .highlight-icon {
        width: 60px;
        height: 60px;
        background: var(--accent-color);
        border-radius: 50%;
        margin: 0 auto 1rem;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5rem;
    }

    .highlight-title {
        font-size: 1.25rem;
        font-weight: 600;
        margin-bottom: 0.5rem;
    }

    .carousel-container {
        position: relative;
        overflow: hidden;
    }

    .carousel-track {
        display: flex;
        transition: transform 0.3s ease;
    }

    .carousel-item {
        flex: 0 0 300px;
        margin-right: 1rem;
    }

    @@media (max-width: 768px) {
        .hero-title

    {
        font-size: 2.5rem;
    }

    .section-title {
        font-size: 2rem;
    }

    .hero-section {
        height: 60vh;
    }

    .carousel-item {
        flex: 0 0 280px;
    }

    }

    @@media (max-width: 576px) {
        .hero-title

    {
        font-size: 2rem;
    }

    .section-padding {
        padding: 3rem 0;
    }

    }
</style>

<!-- Hero Banner -->
<section class="hero-section">
    <div class="hero-content">
        <h1 class="hero-title">Welcome to AuraStore</h1>
        <p class="hero-subtitle">Discover premium products with unmatched quality and style</p>
        <div>
            <a href="#" class="cta-button">Shop Now</a>
            <a href="#" class="cta-button secondary">View Collection</a>
        </div>
    </div>
</section>

<!-- Featured Categories -->
<section class="section-padding">
    <div class="container">
        <h2 class="section-title">Shop by Category</h2>
        
    </div>
</section>

<!-- Flash Sale -->
<section class="section-padding flash-sale">
    <div class="container">
        <div class="text-center">
            <div class="countdown-timer">
                <div class="timer-text">⚡ Flash Sale Ends In: 2h 45m 30s</div>
            </div>
        </div>
        <h2 class="section-title">Top Deals</h2>
        
    </div>
</section>

<!-- Best Sellers -->
<section class="section-padding">
    <div class="container">
        <h2 class="section-title">Best Sellers</h2>
        <div class="row">
            
        </div>
    </div>
</section>

<!-- New Arrivals -->
<section class="section-padding flash-sale">
    <div class="container">
        <h2 class="section-title">New Arrivals</h2>
        <div class="row">
            <div class="col-lg-4 col-md-6 mb-4">
                
            </div>
        </div>
    </div>
</section>

<!-- Brand Highlights -->
<section class="section-padding highlights-section">
    <div class="container">
        <div class="row">
            <div class="col-lg-3 col-md-6">
                <div class="highlight-item">
                    <div class="highlight-icon">🚚</div>
                    <h3 class="highlight-title">Free Shipping</h3>
                    <p>On orders over $50</p>
                </div>
            </div>
            <div class="col-lg-3 col-md-6">
                <div class="highlight-item">
                    <div class="highlight-icon">🔒</div>
                    <h3 class="highlight-title">Secure Payment</h3>
                    <p>100% protected transactions</p>
                </div>
            </div>
            <div class="col-lg-3 col-md-6">
                <div class="highlight-item">
                    <div class="highlight-icon">↩️</div>
                    <h3 class="highlight-title">Easy Returns</h3>
                    <p>30-day return policy</p>
                </div>
            </div>
            <div class="col-lg-3 col-md-6">
                <div class="highlight-item">
                    <div class="highlight-icon">💬</div>
                    <h3 class="highlight-title">24/7 Support</h3>
                    <p>Always here to help</p>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Testimonials -->
<section class="section-padding">
    <div class="container">
        <h2 class="section-title">What Our Customers Say</h2>
        <div class="row">
            
            
        </div>
    </div>
</section>

<script>
    // Simple countdown timer
    function updateCountdown() {
        const now = new Date().getTime();
        const saleEnd = now + 2*60*60*1000 + 45*60*1000 + 30*1000; // 2h 45m 30s from now

        const distance = saleEnd - now;

        const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
        const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
        const seconds = Math.floor((distance % (1000 * 60)) / 1000);

        document.querySelector('.timer-text').innerHTML = `⚡ Flash Sale Ends In: ${hours}h ${minutes}m ${seconds}s`;
    }

    setInterval(updateCountdown, 1000);

    // Smooth scroll for anchor links
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
        anchor.addEventListener('click', function (e) {
            e.preventDefault();
            const target = document.querySelector(this.getAttribute('href'));
            if (target) {
                target.scrollIntoView({
                    behavior: 'smooth',
                    block: 'start'
                });
            }
        });
    });
</script>